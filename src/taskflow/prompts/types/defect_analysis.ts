import type { TypePhasePrompts } from './index';

/**
 * 缺陷分析（defect_analysis）— 各阶段专属提示词
 *
 * 核心动作：理解缺陷表现 → 全链路追溯 → 定位根因 → 评估影响面 → 输出修复方案
 * 聚焦于已确认的缺陷（比问题分析更进一步 —— 已知是 bug，需要定位 + 修）。
 */
export const DEFECT_ANALYSIS_PROMPTS: TypePhasePrompts = {
  goal: `【缺陷分析 — 目标确认】

当前是「缺陷分析」任务。已经确认这是一个需要修复的缺陷。目标是精确描述缺陷并准备追溯。

行为约束：
1. 帮助用户精确描述缺陷：具体表现、触发条件、期望行为 vs 实际行为
2. 区分表象（用户看到的）和可能涉及的模块（代码侧）
3. 明确验收标准：缺陷不再复现、根因已定位、回归测试通过
4. 输出一句话缺陷摘要，20 字以内
5. 当前不写代码，只做缺陷陈述和目标确认`,

  plan: `【缺陷分析 — 追溯计划】

当前是「缺陷分析」任务。计划阶段的核心是设计全链路追溯路径。

行为约束：
1. 以"触发入口 → 中间链路 → 出错点"为主线，画出需要审查的代码路径
2. 列出可能出错的环节（数据校验 / 状态管理 / 边界条件 / 并发 / 外部依赖）
3. 为每个环节设计验证方式：读哪个文件、检查哪个函数、验证什么条件
4. 评估修复风险 —— 修改这个位置可能影响哪些其他功能
5. 不写代码，只制定追溯计划`,

  execute: `【缺陷分析 — 追溯执行】

当前是「缺陷分析」任务。执行阶段的核心是沿调用链追溯，定位根因。

行为约束：
1. ⚠️ 分析阶段只读不改 —— 使用 read、grep、glob、codegraph 追踪代码
2. 从触发入口逐层追踪到出错点，记录完整的调用链（文件:行号:函数名）
3. 验证每个链路上的数据变化 —— 输入什么、经过什么处理、输出什么
4. 找到根因后，评估影响范围（同一模式出现在哪些其他地方）
5. 输出根因定位结果 + 证据链 + 修复方案概述
6. 定位完成后可以输出修复代码建议（markdown 代码块），但不落地文件 —— 等用户确认后再改`,

  self_verify: `【缺陷分析 — 修复自验】

当前是「缺陷分析」任务。自验阶段审查修复方案的完整性和安全性。

行为约束：
1. 验证根因修复后能否覆盖所有触发场景（不只是当前这个 case）
2. 检查修复是否引入回归风险 —— 修改的函数被哪些其他地方调用
3. 确认边界条件处理完整（空值、越界、并发、异常路径）
4. 如果发现遗漏，回去补充
5. 最终确认修复方案可以安全落地`,

  review: `【缺陷分析 — 验收】

当前是「缺陷分析」任务。验收阶段展示完整的缺陷分析报告。

验收报告结构：
1. 📋 缺陷描述（表现、触发条件、期望 vs 实际）
2. 🔍 根因追溯（完整调用链 + 证据，每步标注文件:行号）
3. 🛠️ 修复方案（代码变更说明 + 变更文件清单）
4. ⚠️ 影响面评估（修复影响的其他模块 / 回归风险）
5. ✅ 验收检查（根因已定位、缺陷不再复现、回归测试通过）

行为约束：
- 等待用户验收结论和变更
- 用户可以驳回、要求补充或验收通过`,
};
