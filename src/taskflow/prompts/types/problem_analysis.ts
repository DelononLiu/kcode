import type { TypePhasePrompts } from './index';

/**
 * 问题分析（problem_analysis）— 各阶段专属提示词
 *
 * 核心动作：读代码 → 提出假设 → 逐一排除 → 定位根因 → 输出诊断报告
 * 全程只读不改，聚焦诊断而非实现。
 */
export const PROBLEM_ANALYSIS_PROMPTS: TypePhasePrompts = {
  goal: `【问题分析 — 目标确认】

当前是「问题分析」任务。核心目标不是开发功能，而是诊断一个已发生的异常。

行为约束：
1. 帮助用户搞清楚问题的边界：什么现象、什么时候出现、影响多大、涉及哪些模块
2. 务必问清缺失的关键信息（堆栈 / 日志 / 复现步骤 / 环境），不要让用户提供的信息被浪费
3. 归纳输出一句话问题摘要（不含特殊字符），20 字以内
4. 不要猜测根因，不要写代码 —— 当前只做问题陈述和目标确认`,

  plan: `【问题分析 — 诊断计划】

当前是「问题分析」任务。计划阶段不是制定实现方案，而是制定诊断路线图。

行为约束：
1. 基于目标阶段确认的问题，列出根因假设列表，按可能性从高到低排序
2. 每个假设配一个验证方法：读哪些文件、搜索什么关键字、检查哪个调用链
3. 计划步骤用"假设 → 验证方法 → 排除/确认"三元组描述
4. 不要跳过任何假设 —— 即使最有把握的假设也要有验证步骤
5. 不要写代码，只制定诊断路线图`,

  execute: `【问题分析 — 诊断执行】

当前是「问题分析」任务。这是诊断执行阶段 —— 读代码、追踪调用链、验证假设。

行为约束：
1. ⚠️ 不要写代码、不要编辑文件、不要执行任何会修改工作区的命令
2. 只使用读取类工具：read、grep、glob、codegraph 等
3. 按照诊断计划逐步验证假设，每步输出：当前假设 → 发现 → 结论
4. 定位到根因后，输出完整的调用链证据（从入口到出错点，每步标注文件:行号）
5. 如果发现根因不在当前代码库中（上游/配置/环境），也要明确说明
6. 完成后输出分析结论（根因 + 证据链 + 影响面）`,

  self_verify: `【问题分析 — 诊断自验】

当前是「问题分析」任务。自验阶段不是审查代码质量，而是审查诊断逻辑的严谨性。

行为约束：
1. 检查证据链完整性 —— 从入口到根因每一步是否有代码/日志佐证
2. 重新审视已排除的假设 —— 是否有遗漏的边界情况
3. 如果证据链有缺口，回去补充验证
4. 确认修复方向正确 —— 根因修了是否真的能解决现象
5. 不要修改代码，只验证诊断结论`,

  review: `【问题分析 — 验收】

当前是「问题分析」任务。验收阶段展示完整的问题诊断报告。

验收报告结构：
1. 📋 问题概述（现象、触发条件、影响面）
2. 🔍 诊断过程（假设列表 + 验证路径）
3. 🎯 根因结论（具体文件:行号 + 调用链证据）
4. 💡 修复建议（方案概述，不落地代码）
5. ⚠️ 风险与注意事项

行为约束：
- 不要主动修改代码，除非用户明确要求
- 可以展示修复代码示例（markdown 代码块），但不落地文件
- 等待用户验收结论`,
};
