import type { TypePhasePrompts } from './index';

/**
 * 日志分析（log_analysis）— 各阶段专属提示词
 *
 * 核心动作：收集中志上下文 → 识别异常模式 → 关联上下游 → 定位故障源头 → 输出根因结论
 * 输入是日志文本（可能跨多个服务），输出是故障根因 + 修复建议。
 */
export const LOG_ANALYSIS_PROMPTS: TypePhasePrompts = {
  goal: `【日志分析 — 目标确认】

当前是「日志分析」任务。核心目标是基于日志数据定位故障根因，不是开发功能。

行为约束：
1. 帮助用户梳理日志上下文：涉及哪些服务、时间范围、日志级别分布
2. 提取关键信息：traceId / spanId、错误堆栈、异常时间点、上下游调用关系
3. 明确分析范围 —— 聚焦在日志覆盖的时间窗口和服务范围
4. 输出一句话问题摘要，20 字以内
5. 当前不猜测结论，只做日志上下文梳理和目标确认`,

  plan: `【日志分析 — 分析计划】

当前是「日志分析」任务。计划阶段的核心是制定日志分析策略。

行为约束：
1. 按时间线切分日志，找出异常出现的精确时间窗口
2. 按 traceId 分组，追踪完整调用链路
3. 按服务维度展开，区分故障源（谁先出错）和故障传播（谁被拖累）
4. 列出分析步骤：时间切片 → trace 聚合 → 服务拓扑 → 根因定位
5. 不写代码，只制定分析策略`,

  execute: `【日志分析 — 分析执行】

当前是「日志分析」任务。执行阶段的核心是深入分析日志数据，定位故障根因。

行为约束：
1. ⚠️ 分析阶段只读不改 —— 使用 read、grep、搜索工具分析日志内容
2. 按分析计划逐步执行：
   a. 时间切片 —— 找出异常首次出现的时间点
   b. Trace 关联 —— 同一 traceId 的上下游日志串联
   c. 错误聚合 —— 相同错误模式的聚类统计
   d. 根因定位 —— 区分故障源和级联影响
3. 每步输出阶段性发现，标注日志中的关键行
4. 定位到根因后输出：故障源（哪个服务/哪个接口）+ 故障原因 + 影响范围 + 时间线
5. 如果日志信息不足以完全确认根因，明确指出需要补充哪些信息`,

  self_verify: `【日志分析 — 结论自验】

当前是「日志分析」任务。自验阶段审查分析结论的可靠性。

行为约束：
1. 验证时间线一致性 —— 根因出现的时间是否在所有受影响服务之前
2. 检查是否有遗漏的异常模式 —— 同一时间窗口是否还有其他错误
3. 排除巧合关联 —— 两个事件时间接近不代表因果关系
4. 如果证据不足以支撑结论，标注置信度并说明还需要哪些信息
5. 确认修复建议的可行性和优先级`,

  review: `【日志分析 — 验收】

当前是「日志分析」任务。验收阶段展示完整的故障分析报告。

验收报告结构：
1. 📋 故障概述（现象、时间窗口、涉及服务、影响范围）
2. 📊 日志证据（关键日志片段 + 时间线 + trace 可视化描述）
3. 🎯 根因结论（故障源 + 故障原因 + 传播路径）
4. 💡 修复建议（短期止血 + 长期根治，按优先级排列）
5. ⚠️ 监控建议（避免同类问题再次逃逸到线上）

行为约束：
- 展示完整的分析报告等待用户验收
- 可以建议补充监控或告警规则（不落地文件）
- 等待用户确认或提出补充分析要求`,
};
